<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Human-readable console for development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{requestId}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Use JSON in production, CONSOLE in development -->
    <springProfile name="prod,production">
        <!-- Rolling file appender with JSON format for production -->
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>./logs/application.log</file>
            <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
                <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
                    <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
                        <prettyPrint>false</prettyPrint>
                    </jsonFormatter>
                    <timestampFormat>yyyy-MM-dd'T'HH:mm:ss.SSSZ</timestampFormat>
                    <appendLineSeparator>true</appendLineSeparator>
                    <includeContextName>false</includeContextName>
                </layout>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!-- Daily rollover with size limit -->
                <fileNamePattern>./logs/application.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <!-- Max 10MB per file -->
                <maxFileSize>10MB</maxFileSize>
                <!-- Keep 30 days of history -->
                <maxHistory>30</maxHistory>
                <!-- Total size cap of 1GB -->
                <totalSizeCap>1GB</totalSizeCap>
            </rollingPolicy>
        </appender>
        
        <!-- Console appender with JSON for container environments (optional) -->
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
                <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
                    <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
                        <prettyPrint>false</prettyPrint>
                    </jsonFormatter>
                    <timestampFormat>yyyy-MM-dd'T'HH:mm:ss.SSSZ</timestampFormat>
                    <appendLineSeparator>true</appendLineSeparator>
                    <includeContextName>false</includeContextName>
                </layout>
            </encoder>
        </appender>
        
        <root level="INFO">
            <appender-ref ref="FILE"/>
            <!-- Uncomment below line when deploying to containers for stdout -->
            <!-- <appender-ref ref="JSON_CONSOLE"/> -->
        </root>
    </springProfile>
    
    <springProfile name="default,dev,development,local">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        
        <!-- Debug logging for our package -->
        <logger name="com.arcube.transferaggregator" level="DEBUG"/>
    </springProfile>
    
    <!-- Application-specific logging levels -->
    <logger name="com.arcube.transferaggregator" level="DEBUG"/>
    <logger name="com.arcube.transferaggregator.observability" level="INFO"/>
    
    <!-- Reduce noise from libraries -->
    <logger name="org.springframework.web" level="WARN"/>
    <logger name="org.springframework.boot.actuate" level="WARN"/>
    <logger name="reactor.netty" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
</configuration>

